// Standard headers
#include <vector>
#include <string>
// Bioinfo headers
#include <fundamentals.hpp>
#include <query.hpp>

namespace bioinfo {
    std::vector<AAString> queryUniProt(std::vector<std::string> &vec) {
        std::vector<std::string>::iterator it;
        std::vector<AAString> av;

        for (it = vec.begin(); it != vec.end(); it++) {

        }

        return av;
    }
}